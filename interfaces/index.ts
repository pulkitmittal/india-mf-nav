// You can include shared interfaces/types in a separate file
// and then use them in any component by importing them. For
// example, to import the interface below do:
//
// import { MF } from 'path/to/interfaces';

export type MF = {
  id: number;
  name: string;
  nav?: number;
  date?: string;
};
